import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ProfileEntreprisePage } from './profile-entreprise';

@NgModule({
  declarations: [
    ProfileEntreprisePage,
  ],
  imports: [
    IonicPageModule.forChild(ProfileEntreprisePage),
  ],
})
export class ProfileEntreprisePageModule {}
